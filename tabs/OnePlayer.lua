OnePlayer = class()

function OnePlayer:init(x)
    -- you can accept and set parameters here
    self.x = x
end

function OnePlayer:draw()
    -- Codea does not automatically call this method
end

function OnePlayer:touched(touch)
    -- Codea does not automatically call this method
end
